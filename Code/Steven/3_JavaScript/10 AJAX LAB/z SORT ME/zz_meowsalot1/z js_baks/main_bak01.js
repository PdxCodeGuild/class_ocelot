    // This is an OBJECT.
    var myCat = {
        "name": "Meowsalot",
        "species": "cat",
        "favFood": "tuna"
    }

    // This is an ARRAY - like a 'list' in Python.
    var myFavColors = ["blue", "green", "purple"];


    // This is really crazy - an ARRAY of OBJECTS!!
    // var thePets = [{}, {}, {}]


    // ... but it is too messy to write like that, so we write like this:
    var thePets = [
        {
            "name": "Meowsalot",
            "species": "cat",
            "favFood": "tuna"
        },
        {},
